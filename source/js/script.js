var mySwiper = new Swiper ('.m8-swiper-container', {
    // Optional parameters
    direction: 'horizontal',
    loop: true,

    // Navigation arrows
    navigation: {
      nextEl: '.m8-swiper-button-next',
      prevEl: '.m8-swiper-button-prev',
    },
  })
